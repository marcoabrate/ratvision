"""Bundled environment assets (textures, Blender files).

This package contains the image textures, landmark images, and the
default ``box_messy.blend`` Blender scene used by the ratvision
renderers.  The assets are loaded automatically by
`ratvision.box_environment.default_box_environment()` and by
`ratvision.BlenderRenderer` when no custom environment file is
specified.
"""
